<% include partials/header %>
<div class="ui main text container segment">
    <div class="ui huge header"><%= usuario.nombre + " " + usuario.apellido %></div>
    <div class="ui top attached">
        <div class="item thumbnail">
          <img class="ui centered rounded image size" src="../<%= usuario.foto %>">
          <div class="content">
            <h2>Correo</h2>
            <span><%= usuario.correo %></span>
          </div>
          <div class="content">
            <h2>Edad</h2>
            <span><%= usuario.edad %></span>
          </div>
          <div class="content">
            <h2>Numero</h2>
            <span><%= usuario.numero %></span>
          </div>
        </div>
        <div class="botones">
          <a class="ui purple basic button" href="/u/o">Ofertas</a>
        </div>
        <!-- <div class="botones">
          <form id="delete" action="/u <%= usuario._id %>" method="POST">
            <button class="ui green basic button">Descargar curriculum</button>
          </form>
        </div> -->
    </div>
</div>
<% include partials/footer %>
